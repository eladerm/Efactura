rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Permitir lectura y escritura a usuarios autenticados para logos y certificados
      allow read, write: if request.auth != null;
    }
  }
}